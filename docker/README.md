# Docker Notes

Ce dossier est réservé aux assets Docker avancés (override, scripts buildx, registry mirrors, etc.).
Le projet utilise actuellement:

- `backend/Dockerfile`
- `frontend/Dockerfile`
- `docker-compose.yml`

Tu peux y ajouter ensuite:
- `docker-compose.prod.yml`
- scripts de build multi-arch
- templates de variables d'environnement
