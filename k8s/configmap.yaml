apiVersion: v1
kind: ConfigMap
metadata:
  name: fitness-config
  namespace: fitness
data:
  NODE_ENV: "production"
  PORT: "5000"
  # MongoDB service DNS in the cluster
  MONGO_URL: "mongodb://mongodb:27017/mern_db"
  TOKEN_EXPIRE: "2592000"
