
{
  "name": "sport_devops",
  "private": true,
  "scripts": {
    "bootstrap": "npm --prefix backend install && npm --prefix frontend install",
    "validate:backend": "npm --prefix backend run lint && npm --prefix backend run test:ci",
    "validate:frontend": "npm --prefix frontend run lint && npm --prefix frontend run test:ci && npm --prefix frontend run build",
    "validate": "npm run validate:backend && npm run validate:frontend && docker compose config",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  }
}
